/**
 * File to perform manual tests on the module.
 *
 * @author Liv <lh224hh@student.lnu.se>
 */

// import { AstrologyGenerator } from '../lib/AstrologyGenerator.js'
import { DateManager } from '../lib/DateManager.js'

const dateManager = new DateManager('2023-10-30')
dateManager.getAstrologyGenerator()

    // astrologyGenerator.getElement()
    // astrologyGenerator.getModality()
